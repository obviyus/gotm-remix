import * as stylex from "@stylexjs/stylex";

/**
 * Marks a control whose label and icon shift while the control itself is
 * hovered. Applied by the button or link; read by the elements inside it.
 */
export const control = stylex.defineMarker();
